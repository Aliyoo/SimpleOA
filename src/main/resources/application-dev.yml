spring:
  datasource:
    url: jdbc:mysql://localhost:3306/simpleoa_dev?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: dev_user
    password: dev_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8081
  servlet:
    context-path: /api

logging:
  level:
    root: debug